name: muzik
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  youtube_player_flutter: ^9.1.3
  youtube_explode_dart: ^3.0.5
  palette_generator: ^0.3.3+3 
  equatable: ^2.0.8
  get_it: ^9.2.0
  screenshot: ^3.0.0
  share_plus: ^12.0.1
  path_provider: ^2.1.1
  app_links: ^7.0.0
  firebase_core: ^4.4.0
  firebase_auth: ^6.1.4
  cloud_firestore: ^6.1.2
  firebase_storage: ^13.0.6 
  # Downgrade to a version compatible with the existing usage in
  # lib/services/auth_service.dart (unnamed constructor, .signIn(),
  # and accessToken/idToken properties).
  google_sign_in: ^5.4.0
  http: ^1.2.0
  image_picker: ^1.0.7
  permission_handler: ^12.0.1
  flutter_webrtc: ^1.3.0
  record: ^6.2.0
  just_audio: ^0.10.5
  audio_service: ^0.18.15
  audio_session: ^0.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
